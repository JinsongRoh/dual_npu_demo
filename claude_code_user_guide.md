# 🚀 Claude Code 완벽 사용 가이드

> **버전**: 2.0.76  
> **대상**: 초보자부터 중급자까지  
> **최종 업데이트**: 2024년 12월

---

## 📋 목차

1. [Claude Code란?](#1-claude-code란)
2. [기본 사용법](#2-기본-사용법)
3. [키보드 단축키 완전 정복](#3-키보드-단축키-완전-정복)
4. [슬래시 명령어 (/) 가이드](#4-슬래시-명령어--가이드)
5. [파일 참조하기 (@)](#5-파일-참조하기-)
6. [Bash 모드 (!)](#6-bash-모드-)
7. [모델 선택 및 전환](#7-모델-선택-및-전환)
8. [CLAUDE.md 설정 파일](#8-claudemd-설정-파일)
9. [커스텀 명령어 만들기](#9-커스텀-명령어-만들기)
10. [실전 워크플로우](#10-실전-워크플로우)
11. [고급 기능](#11-고급-기능)
12. [문제 해결 및 팁](#12-문제-해결-및-팁)

---

## 1. Claude Code란?

### 1.1 개요

Claude Code는 Anthropic에서 개발한 **터미널 기반 AI 코딩 어시스턴트**입니다.

```
┌─────────────────────────────────────────────────────────┐
│  Claude Code = AI + 터미널 + 코드베이스 이해 능력       │
│                                                         │
│  ✅ 코드베이스 전체를 분석하고 이해                     │
│  ✅ 파일을 직접 편집 (승인 후)                          │
│  ✅ 터미널 명령어 실행                                  │
│  ✅ Git 워크플로우 관리                                 │
│  ✅ 버그 수정 및 테스트 실행                            │
└─────────────────────────────────────────────────────────┘
```

### 1.2 주요 특징

| 기능 | 설명 |
|------|------|
| **코드베이스 이해** | 프로젝트 전체 구조를 파악하고 맥락을 이해합니다 |
| **파일 편집** | 사용자 승인 후 직접 파일을 수정합니다 |
| **명령어 실행** | 빌드, 테스트, Git 등 터미널 명령을 실행합니다 |
| **대화형 인터페이스** | 자연어로 대화하며 코딩 작업을 수행합니다 |

---

## 2. 기본 사용법

### 2.1 Claude Code 시작하기

```bash
# 기본 실행 (대화형 REPL 모드)
claude

# 초기 프롬프트와 함께 시작
claude "이 프로젝트를 분석해줘"

# 특정 디렉토리에서 시작
cd ~/my-project
claude
```

### 2.2 기본 대화 흐름

```
┌─────────────────────────────────────────────────────────┐
│ orangepi@orangepi5plus:~/project$ claude               │
│                                                         │
│ > 이 프로젝트의 구조를 설명해줘                         │
│                                                         │
│ Claude: 이 프로젝트는 React 기반의 웹 애플리케이션...   │
│                                                         │
│ > src/App.js 파일의 버그를 수정해줘                     │
│                                                         │
│ Claude: [파일 편집 요청]                                │
│ 이 변경사항을 적용할까요? [y/n]                         │
└─────────────────────────────────────────────────────────┘
```

### 2.3 세션 관리

```bash
# 이전 대화 이어서 하기
claude -c

# 특정 세션 재개
claude -r "세션ID" "계속 작업해줘"

# 버전 확인
claude --version
```

---

## 3. 키보드 단축키 완전 정복

### 3.1 필수 단축키 (매일 사용)

| 단축키 | 기능 | 설명 |
|--------|------|------|
| `Esc` | **작업 중지** | Claude가 작업 중일 때 중지 |
| `Esc` + `Esc` | **입력 지우기** | 현재 입력 내용 전체 삭제 |
| `Ctrl + C` | **프로그램 종료** | Claude Code 완전 종료 |
| `↑` / `↓` | **히스토리 탐색** | 이전 입력 명령어 탐색 |

### 3.2 입력 관련 단축키

| 단축키 | 기능 | 설명 |
|--------|------|------|
| `\ + Enter` | **줄바꿈** | 여러 줄 입력 시 사용 |
| `Shift + Enter` | **줄바꿈** | 위와 동일 (터미널에 따라 다름) |
| `Ctrl + _` | **실행취소** | 마지막 입력 취소 |
| `Ctrl + V` | **경로 붙여넣기** | 파일 경로 붙여넣기 |
| `Ctrl + S` | **프롬프트 임시저장** | 현재 입력을 나중에 사용하도록 저장 |

### 3.3 모드 전환 단축키

| 단축키 | 기능 | 설명 |
|--------|------|------|
| `/` | **명령어 모드** | 슬래시 명령어 입력 시작 |
| `!` | **Bash 모드** | 직접 터미널 명령 실행 |
| `@` | **파일 참조** | 특정 파일을 컨텍스트에 추가 |
| `&` | **이미지 첨부** | 이미지 파일 참조 |

### 3.4 고급 단축키

| 단축키 | 기능 | 설명 |
|--------|------|------|
| `Shift + Tab` | **자동 승인 토글** | 편집 자동 승인 모드 전환 |
| `Alt + P` | **모델 전환** | Opus ↔ Sonnet 전환 |
| `Ctrl + O` | **상세 출력** | 디버깅용 상세 로그 표시 |
| `Ctrl + T` | **할일 목록** | TODO 항목 표시 |
| `Ctrl + Z` | **일시 중지** | Claude Code 일시 중지 (백그라운드) |
| `Ctrl + R` | **히스토리 검색** | 이전 명령어 검색 |
| `Tab` | **생각 모드 토글** | 확장된 사고 과정 켜기/끄기 |

### 3.5 단축키 시각화

```
┌─────────────────── 자주 사용하는 단축키 ───────────────────┐
│                                                            │
│   [Esc]        → 작업 중지 / 두 번 누르면 입력 지우기      │
│   [/]          → 슬래시 명령어 시작                        │
│   [!]          → Bash 명령어 직접 실행                     │
│   [@]          → 파일 참조 (예: @src/main.py)              │
│   [Shift+Tab]  → 자동 승인 모드 토글                       │
│   [Alt+P]      → 모델 전환 (Opus ↔ Sonnet)                │
│   [\ + Enter]  → 여러 줄 입력                              │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

---

## 4. 슬래시 명령어 (/) 가이드

### 4.1 필수 명령어

슬래시(`/`)를 입력하면 사용 가능한 명령어 목록이 표시됩니다.

#### 세션 관리

| 명령어 | 설명 | 사용 예 |
|--------|------|---------|
| `/help` | 도움말 표시 | `/help` |
| `/exit` | Claude Code 종료 | `/exit` |
| `/clear` | 대화 기록 삭제 | `/clear` |
| `/compact` | 대화 요약 (토큰 절약) | `/compact 인증 관련 내용만` |

#### 프로젝트 설정

| 명령어 | 설명 | 사용 예 |
|--------|------|---------|
| `/init` | CLAUDE.md 파일 생성 | `/init` |
| `/config` | 설정 패널 열기 | `/config` |
| `/permissions` | 권한 설정 관리 | `/permissions` |
| `/add-dir` | 작업 디렉토리 추가 | `/add-dir ../other-project` |

#### 상태 확인

| 명령어 | 설명 | 사용 예 |
|--------|------|---------|
| `/doctor` | 설치 상태 진단 | `/doctor` |
| `/cost` | 현재 세션 비용 확인 | `/cost` |
| `/model` | 현재 모델 확인/변경 | `/model` |

#### IDE 및 도구 연동

| 명령어 | 설명 | 사용 예 |
|--------|------|---------|
| `/ide` | IDE 연동 관리 | `/ide` |
| `/terminal-setup` | 터미널 설정 최적화 | `/terminal-setup` |
| `/mcp` | MCP 서버 관리 | `/mcp` |

#### 기타 유용한 명령어

| 명령어 | 설명 | 사용 예 |
|--------|------|---------|
| `/theme` | 테마 변경 | `/theme` |
| `/vim` | Vim 모드 활성화 | `/vim` |
| `/review` | 코드 리뷰 요청 | `/review` |
| `/login` | 로그인 | `/login` |
| `/logout` | 로그아웃 | `/logout` |

### 4.2 명령어 사용 예시

```bash
# 새 프로젝트 시작 시 추천 순서
> /init              # 1. CLAUDE.md 생성
> /ide               # 2. IDE 연동 설정
> /terminal-setup    # 3. 터미널 최적화

# 작업 중
> /model             # 모델 확인
> /cost              # 비용 확인

# 세션 정리
> /compact           # 대화 요약
> /clear             # 새로 시작
```

---

## 5. 파일 참조하기 (@)

### 5.1 기본 사용법

`@` 기호를 사용하면 특정 파일을 Claude의 컨텍스트에 추가할 수 있습니다.

```bash
# 단일 파일 참조
> @src/main.py 이 파일의 버그를 찾아줘

# 여러 파일 참조
> @src/auth.py @src/user.py 이 두 파일의 관계를 설명해줘

# 디렉토리 참조
> @src/ 이 폴더의 구조를 분석해줘
```

### 5.2 자동완성 활용

```
> @src/                    [Tab 키로 자동완성]
  ├── main.py
  ├── auth.py
  ├── utils/
  └── config.py
```

### 5.3 파일 참조 팁

```
┌─────────────────── 파일 참조 팁 ───────────────────┐
│                                                     │
│  ✅ 관련 파일만 참조 (불필요한 컨텍스트 줄이기)    │
│  ✅ Tab 키로 자동완성 활용                          │
│  ✅ Ctrl+V로 파일 경로 붙여넣기                     │
│  ✅ 드래그 앤 드롭으로 파일 추가 (IDE 연동 시)     │
│                                                     │
└─────────────────────────────────────────────────────┘
```

---

## 6. Bash 모드 (!)

### 6.1 개요

`!`로 시작하면 Claude를 거치지 않고 **직접 터미널 명령어를 실행**합니다.

```bash
# Claude에게 요청 (토큰 소비)
> git status를 확인해줘

# 직접 실행 (토큰 절약!)
> !git status
```

### 6.2 사용 예시

```bash
# 파일 목록 확인
> !ls -la

# Git 상태 확인
> !git status

# 테스트 실행
> !npm test

# 서버 시작
> !python manage.py runserver

# 패키지 설치
> !pip install requests
```

### 6.3 Bash 모드 vs 일반 요청 비교

| 상황 | 일반 요청 | Bash 모드 |
|------|----------|-----------|
| 파일 목록 확인 | "현재 디렉토리 파일 보여줘" | `!ls -la` |
| Git 상태 | "git 상태 확인해줘" | `!git status` |
| 테스트 실행 | "테스트 실행해줘" | `!npm test` |
| **토큰 사용** | ⚠️ 사용함 | ✅ 사용 안 함 |
| **AI 분석** | ✅ 결과 분석해줌 | ❌ 결과만 표시 |

### 6.4 언제 Bash 모드를 사용할까?

```
┌─────────────────── Bash 모드 사용 가이드 ───────────────────┐
│                                                              │
│  ✅ Bash 모드 추천 상황:                                     │
│     - 단순 명령어 실행 (ls, cd, git status 등)              │
│     - 결과를 직접 확인만 하면 될 때                         │
│     - 토큰을 절약하고 싶을 때                               │
│                                                              │
│  ❌ 일반 요청 추천 상황:                                     │
│     - 명령어 결과를 분석해야 할 때                          │
│     - 어떤 명령어를 써야 할지 모를 때                       │
│     - 복잡한 작업을 자동화할 때                             │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 7. 모델 선택 및 전환

### 7.1 사용 가능한 모델

| 모델 | 특징 | 추천 상황 |
|------|------|----------|
| **Claude Opus** | 최고 성능, 복잡한 추론 | 어려운 버그, 아키텍처 설계 |
| **Claude Sonnet** | 빠르고 효율적 | 일반 코딩, 빠른 응답 필요 시 |

### 7.2 모델 전환 방법

```bash
# 방법 1: 단축키
Alt + P  # 모델 즉시 전환

# 방법 2: 슬래시 명령어
> /model

# 방법 3: 시작 시 지정
claude --model sonnet
claude --model opus
claude --model claude-sonnet-4-20250514  # 특정 버전
```

### 7.3 기본 동작

```
┌─────────────────── 모델 자동 전환 ───────────────────┐
│                                                       │
│  기본 설정:                                           │
│  - 사용량 0~50%: Opus 사용                           │
│  - 사용량 50% 이상: Sonnet으로 자동 전환             │
│                                                       │
│  💡 팁: 복잡한 작업은 Opus로, 간단한 작업은 Sonnet   │
│                                                       │
└───────────────────────────────────────────────────────┘
```

---

## 8. CLAUDE.md 설정 파일

### 8.1 CLAUDE.md란?

CLAUDE.md는 프로젝트별 설정과 지침을 저장하는 파일입니다. Claude가 이 파일을 읽고 프로젝트 컨텍스트를 이해합니다.

### 8.2 파일 생성

```bash
# 자동 생성 (Claude가 프로젝트 분석 후 생성)
> /init

# 수동 생성
touch CLAUDE.md
```

### 8.3 CLAUDE.md 예시

```markdown
# 프로젝트: MyWebApp

## 개요
React + TypeScript 기반 웹 애플리케이션

## 기술 스택
- Frontend: React 18, TypeScript, Tailwind CSS
- Backend: Node.js, Express
- Database: PostgreSQL

## 코딩 컨벤션
- 컴포넌트는 함수형으로 작성
- CSS는 Tailwind 사용
- 변수명은 camelCase
- 파일명은 PascalCase (컴포넌트)

## 빌드 및 실행
- 개발 서버: `npm run dev`
- 빌드: `npm run build`
- 테스트: `npm test`

## 주의사항
- .env 파일은 절대 커밋하지 않음
- API 키는 환경변수로 관리
- 커밋 전 lint 실행 필수
```

### 8.4 CLAUDE.md 위치별 적용 범위

| 위치 | 적용 범위 | 예시 |
|------|----------|------|
| `~/.claude/CLAUDE.md` | 모든 프로젝트 (글로벌) | 개인 코딩 스타일 |
| `프로젝트/CLAUDE.md` | 해당 프로젝트 | 프로젝트별 설정 |
| `프로젝트/.claude/CLAUDE.md` | 해당 프로젝트 | 팀 공유용 설정 |

### 8.5 대화 중 CLAUDE.md에 저장하기

```bash
# # 접두사로 메모 저장
> # 이 프로젝트에서는 항상 TypeScript를 사용해야 함

Claude: "CLAUDE.md에 저장할까요?" [y/n]
```

---

## 9. 커스텀 명령어 만들기

### 9.1 커스텀 명령어란?

자주 사용하는 프롬프트를 슬래시 명령어로 만들어 재사용할 수 있습니다.

### 9.2 명령어 파일 위치

| 위치 | 적용 범위 |
|------|----------|
| `~/.claude/commands/` | 모든 프로젝트 (개인용) |
| `.claude/commands/` | 현재 프로젝트 (팀 공유용) |

### 9.3 기본 커스텀 명령어 만들기

```bash
# 디렉토리 생성
mkdir -p ~/.claude/commands

# 코드 리뷰 명령어 생성
cat > ~/.claude/commands/review.md << 'EOF'
이 코드를 리뷰해줘. 다음 항목을 확인해:
- 버그 가능성
- 성능 문제
- 보안 취약점
- 코드 스타일
EOF

# 최적화 명령어 생성
cat > ~/.claude/commands/optimize.md << 'EOF'
이 코드의 성능을 분석하고 최적화 방안을 제시해줘:
- 시간 복잡도 분석
- 메모리 사용량
- 병목 지점 식별
- 개선 코드 제안
EOF
```

### 9.4 사용 방법

```bash
# 명령어 사용
> /review @src/main.py
> /optimize @src/algorithm.py
```

### 9.5 인자가 있는 커스텀 명령어

```markdown
<!-- ~/.claude/commands/fix-issue.md -->
---
argument-hint: [이슈번호]
description: GitHub 이슈 수정
---
GitHub 이슈 #$ARGUMENTS 를 확인하고 수정해줘.
코딩 표준을 따르고, 테스트도 작성해줘.
```

사용:
```bash
> /fix-issue 123
```

### 9.6 고급 커스텀 명령어

```markdown
<!-- ~/.claude/commands/commit.md -->
---
allowed-tools: Bash(git add:*), Bash(git status:*), Bash(git commit:*)
description: Git 커밋 생성
---
## 현재 상태
- Git 상태: !`git status`
- 변경 내용: !`git diff HEAD`
- 현재 브랜치: !`git branch --show-current`

위 변경사항을 분석하고 의미있는 커밋 메시지를 작성해줘.
```

---

## 10. 실전 워크플로우

### 10.1 새 프로젝트 시작하기

```bash
# 1. 프로젝트 디렉토리로 이동
cd ~/my-new-project

# 2. Claude Code 시작
claude

# 3. 프로젝트 초기화
> /init

# 4. IDE 연동 (선택사항)
> /ide

# 5. 프로젝트 분석 요청
> 이 프로젝트의 구조와 목적을 설명해줘
```

### 10.2 버그 수정 워크플로우

```bash
# 1. 에러 로그 확인
> !cat error.log

# 2. Claude에게 분석 요청
> @error.log 이 에러의 원인을 분석하고 수정해줘

# 3. 수정사항 확인
> !git diff

# 4. 테스트 실행
> !npm test

# 5. 커밋
> 변경사항을 커밋해줘
```

### 10.3 코드 리뷰 워크플로우

```bash
# 1. 변경된 파일 확인
> !git status

# 2. 리뷰 요청
> @src/feature.py 이 코드를 리뷰해줘

# 3. 피드백 반영
> 지적한 부분을 수정해줘

# 4. PR 생성
> PR을 생성해줘
```

### 10.4 리팩토링 워크플로우

```bash
# 1. Plan 모드로 계획 수립 (Shift+Tab 두 번)
> 이 모듈을 리팩토링하고 싶어. 계획을 세워줘

# 2. 계획 검토 및 승인

# 3. 단계별 실행
> 1단계부터 시작해줘

# 4. 각 단계 테스트
> !npm test
```

### 10.5 일일 개발 루틴

```
┌─────────────────── 일일 개발 루틴 ───────────────────┐
│                                                       │
│  아침:                                                │
│  1. claude -c    (이전 작업 이어서)                  │
│  2. !git pull    (최신 코드 받기)                    │
│  3. /cost        (사용량 확인)                       │
│                                                       │
│  작업 중:                                             │
│  - /clear 자주 사용 (새 작업마다)                    │
│  - Alt+P로 모델 적절히 전환                          │
│  - @파일명으로 필요한 파일만 참조                    │
│                                                       │
│  마무리:                                              │
│  1. 변경사항 커밋                                     │
│  2. /compact (다음에 이어서 하려면)                  │
│  3. /cost (비용 확인)                                │
│                                                       │
└───────────────────────────────────────────────────────┘
```

---

## 11. 고급 기능

### 11.1 자동 승인 모드

매번 편집 승인을 물어보는 것이 번거로울 때:

```bash
# 방법 1: 단축키로 토글
Shift + Tab  # 여러 번 누르면 모드 전환

# 모드 순서:
# 일반 모드 → 자동 승인 → Plan 모드 → 일반 모드

# 방법 2: 시작 시 설정
claude --dangerously-skip-permissions
```

### 11.2 Plan 모드 (안전한 분석)

코드 변경 없이 계획만 세우고 싶을 때:

```bash
# Shift+Tab 두 번 눌러서 Plan 모드 진입
> 이 프로젝트를 마이크로서비스로 분리하려면 어떻게 해야 할까?

# Claude가 분석만 하고 실제 변경은 하지 않음
```

### 11.3 확장된 사고 모드 (Thinking Mode)

복잡한 문제에 더 깊은 분석이 필요할 때:

```bash
# 키워드 사용 (토큰 소비량 순)
> Think. 인증 모듈 리팩토링 계획을 세워줘         # 기본
> Think harder. REST에서 gRPC로 마이그레이션해줘  # 중간
> Ultrathink. 결제 테스트 오류를 분석해줘         # 최대 (31,999 토큰)
```

### 11.4 백그라운드 실행

오래 걸리는 작업을 백그라운드에서 실행:

```bash
# 백그라운드로 보내기
Ctrl + B  # 실행 중인 명령을 백그라운드로

# 또는 프롬프트로 요청
> 이 빌드를 백그라운드에서 실행해줘
```

### 11.5 파이프라인 활용

```bash
# 로그 분석
cat error.log | claude -p "이 에러를 분석해줘"

# 커밋 요약
git log --oneline -10 | claude -p "최근 커밋을 요약해줘"

# 디렉토리 설명
ls -la | claude -p "이 파일들을 설명해줘"
```

### 11.6 JSON 출력 (스크립팅용)

```bash
# JSON 형식 출력
claude -p "프로젝트 분석해줘" --output-format json

# 스트리밍 JSON
claude -p "큰 작업 실행" --output-format stream-json
```

### 11.7 MCP (Model Context Protocol) 서버

외부 서비스와 연동:

```bash
# MCP 서버 관리
> /mcp

# 사용 가능한 MCP 서버 예시:
# - GitHub
# - Slack
# - Google Drive
# - Jira
```

---

## 12. 문제 해결 및 팁

### 12.1 자주 발생하는 문제

#### Claude가 너무 많은 권한을 요청할 때

```bash
# 특정 도구만 허용
claude --allowedTools "Bash(git log:*)" "Bash(git diff:*)" "Write"

# 특정 도구 차단
claude --disallowedTools "Bash(rm:*)" "Bash(sudo:*)"
```

#### 토큰을 너무 많이 사용할 때

```bash
# 대화 압축
> /compact

# 대화 초기화 (새 작업 시작 전)
> /clear

# 비용 확인
> /cost
```

#### Claude가 느릴 때

```bash
# Sonnet으로 전환
Alt + P

# 또는
> /model sonnet
```

### 12.2 성능 최적화 팁

```
┌─────────────────── 성능 최적화 팁 ───────────────────┐
│                                                       │
│  💡 토큰 절약:                                        │
│  - /clear 자주 사용                                   │
│  - ! 로 단순 명령 직접 실행                          │
│  - 필요한 파일만 @로 참조                            │
│  - /compact로 긴 대화 요약                           │
│                                                       │
│  💡 속도 향상:                                        │
│  - 간단한 작업은 Sonnet 사용                         │
│  - 불필요한 MCP 서버 비활성화                        │
│  - 컨텍스트 창의 80% 이하 유지                       │
│                                                       │
│  💡 정확도 향상:                                      │
│  - CLAUDE.md 잘 작성                                 │
│  - 구체적인 지시 ("~해줘" 보다 상세하게)            │
│  - Plan 모드로 먼저 계획                             │
│                                                       │
└───────────────────────────────────────────────────────┘
```

### 12.3 권장 설정

```bash
# ~/.claude/settings.json
{
  "model": "claude-sonnet-4-20250514",
  "permissions": {
    "allowedTools": ["Read", "Write", "Bash(git *)"],
    "deny": [
      "Read(./.env)",
      "Read(./.env.*)",
      "Write(./production.config.*)"
    ]
  }
}
```

### 12.4 유용한 환경변수

```bash
# ~/.bashrc 또는 ~/.zshrc에 추가

# 자동 업데이트 비활성화
export DISABLE_AUTOUPDATER=1

# 텔레메트리 비활성화
export DISABLE_TELEMETRY=1

# 비용 경고 유지
export DISABLE_COST_WARNINGS=0
```

---

## 📚 빠른 참조 카드

### 필수 단축키

| 단축키 | 기능 |
|--------|------|
| `Esc` | 작업 중지 |
| `Esc` × 2 | 입력 지우기 |
| `/` | 명령어 |
| `!` | Bash 모드 |
| `@` | 파일 참조 |
| `Shift+Tab` | 자동승인 토글 |
| `Alt+P` | 모델 전환 |
| `\ + Enter` | 줄바꿈 |

### 필수 명령어

| 명령어 | 기능 |
|--------|------|
| `/help` | 도움말 |
| `/clear` | 대화 초기화 |
| `/init` | CLAUDE.md 생성 |
| `/cost` | 비용 확인 |
| `/doctor` | 설치 진단 |
| `/exit` | 종료 |

### 추천 워크플로우

```bash
# 새 작업 시작
/clear → 요청 → 확인 → 커밋

# 복잡한 작업
Shift+Tab×2 (Plan모드) → 계획 → 실행

# 빠른 확인
!git status → !npm test
```

---

## 🔗 참고 자료

- **공식 문서**: https://code.claude.com/docs/en/overview
- **Anthropic Console**: https://console.anthropic.com
- **GitHub Issues**: https://github.com/anthropics/claude-code/issues

---

> **마지막 업데이트**: 2024년 12월  
> **Claude Code 버전**: 2.0.76  
> **테스트 환경**: Ubuntu 24.04, Orange Pi 5 Plus (ARM64)
